@model IEnumerable<LiveCore.Models.HistoricoProposta>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DataCriacao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PropostaID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DataCriacao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PropostaID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HistoricoPropostaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HistoricoPropostaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HistoricoPropostaID })
        </td>
    </tr>
}

</table>
